{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "rm -rf flutter && git clone --branch stable https://github.com/flutter/flutter.git && ./flutter/bin/flutter --version && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter precache --web",
  "buildCommand": "./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",

  "crons": [
    {
      "path": "/api/backup",
      "schedule": "0 18 * * *"
    }
  ]
}
