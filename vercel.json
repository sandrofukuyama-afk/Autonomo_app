{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "installCommand": "if [ -d flutter ]; then git -C flutter fetch --depth 1 origin stable && git -C flutter checkout stable; else git clone --depth 1 --branch stable https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter config --enable-web && ./flutter/bin/flutter precache --web",
  "buildCommand": "./flutter/bin/flutter build web --release",
  "outputDirectory": "build/web",

  "crons": [
    {
      "path": "/api/backup",
      "schedule": "0 18 * * *"
    }
  ]
}
