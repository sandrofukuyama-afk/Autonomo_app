name: autonomo_app
description: Aplicativo de controle financeiro para autônomos.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.6
  intl: ^0.20.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
